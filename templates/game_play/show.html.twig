{% extends 'base.html.twig' %}

{% block title %}GamePlay{% endblock %}

{% block body %}
    <h1>GamePlay</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_play.id }}</td>
            </tr>
            <tr>
                <th>NamePlay</th>
                <td>{{ game_play.namePlay }}</td>
            </tr>
            <tr>
                <th>Definition</th>
                <td>{{ game_play.definition }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('game_play_index') }}">back to list</a>

    <a href="{{ path('game_play_edit', {'id': game_play.id}) }}">edit</a>

    {{ include('game_play/_delete_form.html.twig') }}
{% endblock %}
